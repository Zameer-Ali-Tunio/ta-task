# Important: Install @nestjs/config

Before running the backend, you need to install the @nestjs/config package:

```bash
cd backend
npm install @nestjs/config
```

This package is required for loading environment variables from .env files.
